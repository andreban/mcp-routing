---
trigger: always_on
description: "Ensure workspace agent customizations, rules, and skills use the .agents/ directory naming convention."
---

# Workspace Customization Directory Naming Conventions

- Always store workspace customizations, rules, and skills in `.agents/` (e.g., `.agents/rules/`, `.agents/skills/`). Never use `_agents/`.
